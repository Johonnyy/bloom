"""Bloom — define reusable agent configurations, and run them for other agents.

Rename this package if you like — nothing imports it by string — but the
Dockerfile, docker-compose files and Makefile all say ``app.main:app``, so change
those together if you do.
"""
